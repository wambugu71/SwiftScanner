/*
GestureDetector(
                  child: Chip(avatar: Icon(Icons.pause), label: Text('Pause')),
                  onTap: () {
                    ScaffoldMessenger.of(context).showSnackBar(
                      SnackBar(
                        content: Text('QR Code Pause'),
                        duration: Duration(seconds: 2),
                      ),
                    );
                  },
                )
                */
